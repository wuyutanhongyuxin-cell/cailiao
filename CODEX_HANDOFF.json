{
  "status": "implemented_by_claude",
  "task_id": "cailiao-stage2b-real-query-public-doit-intake-review-fix-v1",
  "phase": "Stage 2B line 97 review fix: tighten validate_artifact() in the public DoIT intake tool to verify set_hash against the ordered per-case query_sha256 values (tamper detection). Continuation of the uncommitted intake task; NO real DoIT artifact produced (no network); all five Stage 2B parent blockers REMAIN UNCHECKED.",
  "baseline": "5fb296b91838d31a4d1e80bc76f6955e23acb706 (Add Stage 2B evidence package validator); intake tooling + this fix are uncommitted in the working tree",
  "summary": "Addressed the Codex review finding on tools/prepare_stage2b_real_query_set.py: validate_artifact() now (1) requires set_hash to exist and be a 'sha256:' string, and (2) when all per-case query_sha256 values are present, requires set_hash == _sha256('|'.join(case['query_sha256'] for case in cases)) — the same computation build_artifact uses — failing validation on mismatch. This closes a tamper gap where cases could be reordered/edited without set_hash detecting it (verified: reordering cases now fails validation). Two focused tests were added (tamper-only-set_hash fails; missing set_hash fails). Deterministic and stdlib-only. No real DoIT artifact was produced (WSL has no network); all five Stage 2B parent blockers remain unchecked.",
  "changed_files": [
    "tools/prepare_stage2b_real_query_set.py",
    "tests/test_stage2b_real_query_public_doit_intake.py",
    "tests/data/doit_fixture.sample.jsonl",
    "docs/STAGE2B_REAL_QUERY_PUBLIC_DOIT_INTAKE.md",
    "tools/run_quality_gates.py",
    "README.md",
    "docs/ROADMAP.md",
    "CODEX_HANDOFF.json"
  ],
  "implemented": [
    "tools/prepare_stage2b_real_query_set.py: validate_artifact() now validates set_hash is a sha256 string and, when per-case hashes are all present, equals _sha256('|'.join(case query_sha256 values)); mismatch fails validation. build_artifact()/select/prepare/validate behavior otherwise unchanged and still deterministic + stdlib-only.",
    "tests/test_stage2b_real_query_public_doit_intake.py: added test_tampered_set_hash_fails (tamper ONLY set_hash -> fail) and test_missing_set_hash_fails; existing 17 tests retained (19 total).",
    "Prior intake deliverables from cailiao-stage2b-real-query-public-doit-intake-v1 remain in the working tree (tool, invented fixture, doc, py-compile wiring, README/ROADMAP sub-notes)."
  ],
  "not_implemented": [
    "NO real DoIT artifact was produced: the WSL session still has no outbound network/DNS, so the public dataset could not be downloaded; per the hard rule the artifact was NOT faked.",
    "All five Stage 2B real parent blockers (ROADMAP lines 97/100/103/107/114) remain unchecked by design.",
    "No provider result, model call, eval, telemetry, approval, or production readiness fabricated. No network, no credential/.env read, no git push."
  ],
  "test_files": [
    "tests/test_stage2b_real_query_public_doit_intake.py"
  ],
  "required_test_commands_for_codex": [
    "python -m unittest tests.test_stage2b_real_query_public_doit_intake -v",
    "python -m unittest discover -s tests -v",
    "python tools/run_quality_gates.py --json",
    "python -m json.tool CODEX_HANDOFF.json",
    "git diff --check"
  ],
  "test_status": [
    "python -m unittest tests.test_stage2b_real_query_public_doit_intake -v: 19 tests OK (17 prior + 2 new set_hash tests).",
    "python -m unittest discover -s tests -v: 709 tests OK.",
    "manual check: a valid artifact still passes; reordering cases now fails with a set_hash mismatch error.",
    "python tools/run_quality_gates.py --json (default 'python' on PATH): passed=true, all 5 gates passed; unittest gate ran 709 tests OK.",
    "python -m json.tool CODEX_HANDOFF.json: valid JSON.",
    "git diff --check: no whitespace/conflict issues."
  ],
  "roadmap_item_status": "ALL FIVE STAGE 2B REAL PARENT BLOCKERS UNCHECKED (lines 97/100/103/107/114). Only the checked sub-note under line 97 for the public DoIT intake tooling remains; this fix hardens that tooling's validator.",
  "risks": [
    "set_hash equality is only enforced when every per-case query_sha256 is present and well-formed; if some case hashes are missing/malformed those cases already error, and the set_hash equality check is skipped (avoids a misleading second error) — the artifact still fails validation via the per-case errors.",
    "The intake still produces CANDIDATE evidence only (public human-authored prompts); it does not by itself complete line 97 (relevance targets/qrels, anonymization, readiness sign-off still required).",
    "No real artifact exists in-repo because there was no network; a real artifact must be generated later on a networked machine per the docs.",
    "Environment note: the local interpreter is python3 (no bare 'python' on PATH); the quality-gate runner honors PYTHON=python3, and a local 'python->python3' PATH shim lets its default-interpreter meta-test pass."
  ],
  "credentials": [
    "No credentials or .env files were read, created, or modified.",
    "No external network calls, provider calls, model downloads, or eval runs were used for implementation or local tests.",
    "No push performed; commit/push left to the maintainer."
  ]
}
